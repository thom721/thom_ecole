# PRD — flutter_version (Client desktop Lekol360, réécriture Flutter)

## 1. Contexte et problème

`school_client` (PySide6/Python) est le client desktop historique de Lekol360, mais son code concentre une dette importante (`Controllers/Main.py` ~14k lignes, `Models/enregistrement.py` ~28k lignes, packaging dual Nuitka/PyInstaller — voir `docs/school_client-PRD.md` §6) et reste coûteux à étendre. `flutter_version` est une réécriture complète du même client, consommant la même API `ecole_nginx`, avec deux objectifs simultanés : **fidélité fonctionnelle** au bureau existant (mêmes écrans, mêmes règles métier, mêmes routes API) et **modernisation du rendu** (esthétique reprise du frontend web `ecole_nginx/frontend` — `AdminLayout.vue` — plutôt que du Qt natif du bureau). Méthodologie de travail établie pour ce projet : ne jamais styliser/câbler une fonctionnalité sans avoir d'abord vérifié son comportement réel dans `school_client` (recherche du composant Qt par son CSS-ID) et dans `ecole_nginx` (route/permission réelle) — jamais par supposition.

## 2. Objectifs produit

1. Offrir une alternative desktop (Windows/Mac/Linux, via les cibles Flutter natives déjà scaffoldées dans `windows/`, `macos/`, `linux/`) au client PySide6, sans régression fonctionnelle sur les flux critiques (paiements, ventes, notes, présences, rapports).
2. Reproduire fidèlement les règles métier et permissions déjà imposées par `ecole_nginx`, plutôt que de les réinventer côté client.
3. Adopter une UI plus moderne et cohérente avec le web (`AdminLayout.vue`) tout en conservant les libellés et la hiérarchie du menu du bureau (`kMainNavItems`/`kSecondaryNavItems`, `lib/screens/shell/app_shell.dart`).
4. Étendre — pas seulement reproduire — les mécanismes de sécurité existants quand des limites réelles sont identifiées (ex : flux d'autorisation par PIN, voir §7 ter).
5. Conserver une architecture client plus simple à maintenir que le bureau : un seul module HTTP (`lib/core/api_client.dart`, `Dio`) au lieu des ~8 implémentations dupliquées identifiées dans `school_client` (`docs/school_client-PRD.md` §6).

## 3. Utilisateurs cibles / personas

Identiques à `school_client` (même backend, même matrice rôle→permissions) : Personnel d'accueil/secrétariat, Caissier, Responsable financier, Comptable, Professeur, Responsable des admissions, Responsable pédagogique, Administrateur/Direction. La visibilité du menu est strictement pilotée par le rôle (`AuthState.roleNavItems`, `lib/state/auth_state.dart:30-75`) — ex. un `teacher` ne voit que Notes/Présences/Profile/Actualiser, un `Caissier` voit Paiement/Finances/Rapport/Étudiant/Profile, seul `admin` voit la totalité du menu (Administration, Promus, Log, Abonnement inclus).

## 4. Fonctionnalités (epics & user stories)

Périmètre constaté dans `lib/screens/shell/app_shell.dart` : **chaque entrée du menu principal et secondaire a un écran réel** (aucun ne retombe sur `PlaceholderScreen`, qui ne sert que de garde-fou pour un id de nav inconnu) — la couverture fonctionnelle du bureau est donc déjà large.

### 4.1 Connexion & configuration poste
- En tant qu'utilisateur, je configure l'IP/domaine du serveur au premier lancement (`lib/core/ip_storage.dart`, utilisé par `login_screen.dart`).
- En tant qu'utilisateur, je me connecte avec mes identifiants (`auth/login`, `login_as: as_desktop` — même valeur spéciale que `school_client` pour que le serveur accepte tout `userable_type`).
- En tant qu'utilisateur, mon token reste stocké de façon persistante (`lib/core/token_storage.dart`) entre les lancements.

### 4.2 Dashboard
- En tant que direction, je visualise à l'ouverture un tableau de bord (effectifs, paiements, graphiques — `fl_chart`) équivalent à celui du bureau.

### 4.3 Administration, Étudiants, Professeurs, Cours, Promus
- En tant qu'administrateur/personnel, je gère les étudiants (dossier, recherche, changement de classe), les professeurs, les cours, et je déclenche les promotions de fin d'année — mêmes routes que `school_client`/`ecole_nginx`.
- En tant que personnel, j'édite le badge d'un étudiant avec capture caméra (USB via `camera_macos`, ou IP — `lib/screens/etudiant/badge_screen.dart`, équivalent de `toggle_camera()`/`active_camera_ip()` du bureau). **Écart volontaire** : pas de détection automatique de visage par IA ici (présente côté bureau, voir §5) — le recadrage reste manuel.
- En tant que personnel, je construis mes propres gabarits de badge (positionnement libre, style Canva) via un éditeur visuel dédié, sans équivalent côté bureau ni web — voir §7 septies.

### 4.4 Notes & Présences
- En tant que professeur, je saisis les notes par cours/évaluation et fais l'appel quotidien (`note_state.dart`, `presence_state.dart`), avec les mêmes contraintes de séquence que l'API impose déjà.
- En tant qu'administrateur, je pointe l'arrivée/le départ du personnel (professeurs et personnel) et consulte l'historique par personne/mois (`pointage_tab.dart`, §7 quinquies).

### 4.5 Paiement
- En tant que caissier, j'enregistre un paiement d'écolage, j'imprime un reçu PDF, et je consulte le détail d'un dossier de paiement (versements, retours).
- En tant que rôle sans la permission "Supprimer paiement" (Caissier, etc.), je peux quand même effectuer un **retour de paiement** en saisissant un motif (20-150 caractères) et, si besoin, le PIN d'un admin/Comptable — voir §7 ter pour le détail du mécanisme.

### 4.6 Finances (Vente, Produits, Dépenses, Prêts, Payroll, Autres transactions)
- En tant que caissier, je compose une vente à partir d'un catalogue de produits enregistrés (clic pour ajouter au panier, quantité +/-) — **écart volontaire et explicite** par rapport au bureau, qui saisit nom/catégorie/prix en texte libre à chaque ligne (`vente_composer_screen.dart`).
- En tant que caissier, je clique sur une ligne du tableau des ventes pour rouvrir ce même panneau en mode édition, modifier les lignes ou en supprimer une, sous réserve d'autorisation (§7 ter) — reproduit `on_row_clicked_vente_()`/`vente_show()` du bureau (`Controllers/Main.py:5240-5253`).
- En tant que comptable, j'enregistre une dépense ou une autre transaction ponctuelle, je gère des prêts (taux, remboursement) et la paie du personnel (payroll — fixe ou horaire, voir §7 quinquies).
- Toute suppression de vente/dépense/transaction, et toute modification d'une vente/dépense/transaction déjà enregistrée, suit désormais le même flux motif + autorisation que le retour de paiement (§7 ter).

### 4.7 Rapports
- En tant que direction, je génère les mêmes rapports PDF/Excel que le bureau (financier, pédagogique, paiement, export), plus l'emploi du temps par classe, la charge d'enseignement d'un professeur, un rapport Payroll et l'historique des salaires (§7 quinquies).

### 4.8 Profile, Rôles & Permissions
- En tant qu'utilisateur, je modifie mes informations et mon mot de passe (`lib/screens/profile/profile_screen.dart`, repris du web `adProfile.vue` faute d'équivalent sur le bureau).
- En tant qu'administrateur, je gère les rôles et l'assignation de permissions (`role_assignment_tab.dart`, `permission_assignment_tab.dart`).
- En tant qu'admin/Comptable, je définis ou modifie mon PIN d'autorisation à 6 chiffres depuis mon profil (§7 ter).

### 4.9 Log
- En tant qu'administrateur, je consulte le journal d'activité, avec pour chaque entrée le motif (`reason`) et l'auteur de l'autorisation distincts de l'auteur de l'action quand applicable (§7 ter).

### 4.10 Paramètres
- En tant qu'administrateur, je configure examens, facultés, années académiques, classes, paramètres de paiement, frais et frais divers.

### 4.11 Abonnement
- En tant qu'administrateur, je consulte le statut de mon abonnement et son historique (même route `GET /api/v1/abonnement` que `school_client`, voir `docs/ecole_nginx-PRD.md` §4.7).

## 5. Hors périmètre actuel (constaté dans le code)

- **Détection de visage par IA** pour le recadrage de badge : présente côté `school_client`, absente ici (aucune dépendance ML/vision dans `pubspec.yaml`) — le recadrage est manuel.
- **Mode hors-ligne / cache local** : comme `school_client`, aucune synchronisation différée ; dépendance totale à la connexion au serveur.
- **Flux d'autorisation par PIN (§7 ter)** : câblé uniquement sur Paiement/Vente/Dépense/Transaction (les actions explicitement demandées) — pas encore étendu à d'autres suppressions/modifications sensibles de l'app si elles existent ailleurs (étudiants, notes...).
- **Frontend web Vue.js (`ecole_nginx/frontend`)** : n'a aucune intégration du flux PIN — seuls le bureau et `flutter_version` l'exposent actuellement.

## 6. Risques / dette technique identifiée

- Couverture de tests automatisés inégale : le squelette par défaut `flutter create` ne couvrait longtemps rien de réel, mais l'éditeur de badge (§7 septies) et la suppression de notes (§7 octies) ont depuis introduit des fichiers `test/` réels (modèle, géométrie, widgets, état/réseau via un faux `HttpClientAdapter`) — le reste de l'app (paiements, présences...) continue de reposer sur la revue manuelle et `flutter analyze`. À étendre si le rythme de régressions le justifie ailleurs.
- **Limite d'environnement de build connue** (pas un bug applicatif) : `test/widgets/badge_layout_renderer_test.dart` (rendu PNG bout-en-bout de l'éditeur de badge, `dart:ui Picture.toImage()`) ne se termine jamais dans certains environnements CI/sandbox sans affichage réel — exclu des runs automatisés ; les tests géométriques purs (`Path.contains()`, sans rastérisation) couvrent la même logique sans ce blocage.
- Couplage fort entre certains écrans et leur `State` Provider correspondant (1:1, ex. `VenteTab`↔`VenteState`) : cohérent avec le choix d'architecture (Provider + ChangeNotifier), mais à surveiller si le nombre d'écrans continue de croître.
- **Bug corrigé** — `_AppShellState.dispose()` (`screens/shell/app_shell.dart`) relisait `context.read<AuthState>()` au moment du démontage pour arrêter le minuteur d'inactivité/le watcher d'onglets ; Flutter interdit toute recherche d'ancêtre sur un élément déjà désactivé ("Looking up a deactivated widget's ancestor is unsafe"), ce qui pouvait lever une assertion lors d'un démontage groupé de l'arbre. La référence est désormais capturée dans `didChangeDependencies()` (élément encore actif) et réutilisée dans `dispose()`.

## 7. Mise à jour — build multiplateforme (état)

Les répertoires `windows/`, `macos/`, `linux/` sont déjà scaffoldés (cibles Flutter natives activées) — un pipeline CI dédié à la production d'exécutables pour les trois plateformes reste à mettre en place (hors périmètre de ce document, à traiter séparément si demandé).

## 7 bis. Mise à jour — panneau de vente, parité d'édition (livré)

- Clic sur une ligne du tableau des ventes (`vente_tab.dart`) → ouverture du panneau de composition en mode édition (`VenteComposerScreen(editing: ...)`), pré-rempli via `GET order-vente/{vente}` — reproduit le flux du bureau (`on_row_clicked_vente_()`/`vente_show()`).
- **Bug corrigé** : l'ancien bouton de suppression par ligne du tableau principal utilisait `Vente.order_itemId` (un code à 6 chiffres généré sur la vente elle-même) comme s'il s'agissait de l'id réel d'un `OrderItem` — il ne supprimait jamais la bonne ligne. La suppression se fait désormais à l'intérieur du panneau, sur l'id réel de chaque ligne.
- Décrémenter une ligne déjà enregistrée jusqu'à 0 (boutons +/- ou saisie manuelle de quantité) déclenche désormais le même flux motif + autorisation qu'une suppression explicite, au lieu de retirer silencieusement la ligne sans contrôle.
- **Bug corrigé** : ouvrir le panneau en mode édition levait `setState() or markNeedsBuild() called during build` (`VenteState.clearCart()`/`loadForEdit()` appelés depuis `initState()`, qui appellent `notifyListeners()` pendant la construction du widget). Corrigé en reportant ces appels après le premier frame (`WidgetsBinding.instance.addPostFrameCallback`).
- **Bug corrigé — plafond de stock absent en édition** : `GET order-vente/{vente}` (`RVente.py show_order_items`) ne renvoyait pas `produit_id` dans sa réponse, donc une ligne rechargée en édition n'était jamais reliée à sa fiche produit et n'avait aucun plafond de stock (`stockDisponible`). Corrigé en deux temps : la route renvoie désormais `produit_id`, et `VenteState.applyStockCaps()` (appelée juste après `loadForEdit()`, dans `vente_composer_screen.dart`) résout `stockDisponible` pour chaque ligne depuis le catalogue déjà chargé (`ProduitState.items`).
- **Bug corrigé — suppression de ligne de vente impossible** : `DELETE /order_item` (`destroy_order_item`, `RVente.py:238-240`) typait `order_item_id`/`vente_id` en `int`, alors que `OrderItem.id`/`Vente.id` sont des UUID (`CHAR(36)`) — toute suppression échouait avec une erreur de validation Pydantic (`"Input should be a valid integer, unable to parse string as an integer"`). Pré-existant, jamais exercé avant l'ajout du panneau d'édition (aucune UI n'appelait cette route auparavant). Corrigé en retypant les deux paramètres en `str`.

## 7 quater. Mise à jour — Communauté, contrôle d'accès par onglets, déconnexion forcée (livré)

- **Communauté** : `NavItem('communaute', 'Communauté', Icons.people_outline)` ajouté à `kMainNavItems` ; `'communaute'` avec 3 sous-items (`evenements`, `actualites`, `annonces`) ajouté à `kSubNavItems` dans `app_shell.dart`.
- **Déconnexion forcée sur modification des droits** : `auth_state.dart` ajoute `startTabWatcher()` / `stopTabWatcher()` (polling `/verify-token` toutes les 2 minutes, comparaison par set de `tab_ids`). `app_shell.dart` écoute les changements via `addListener` et affiche `_TabsModifiedDialog` (amber, compte à rebours 10 s, bouton de déconnexion immédiate) — même conception que `_InactivityDialog` déjà en place. `logout()` stoppe le watcher et réinitialise `tabsModified`/`_tabSnapshot`.

## 7 ter. Mise à jour — autorisation par PIN (double approbation) (livré)

Voir `ecole_nginx/PRD.md` §4.3 "S5" pour le détail complet du mécanisme côté serveur (`verify_dual_auth`/`DualAuthChecker`, `User.code_pin`, `Log.reason`/`Log.authorization_id`, contrainte de non-divulgation sur l'unicité des PIN). Côté `flutter_version` :

- `lib/core/dual_auth.dart` centralise le flux : `runWithPinApproval()` (rejoue l'action avec un `approval_token` obtenu via `POST auth/autorisation-access-pin` si le serveur répond 202) et `showReasonDialog()` (motif obligatoire, 20-150 caractères).
- Câblé sur : retour de paiement (`paiement_detail_screen.dart`), suppression de ligne de vente/dépense/transaction, modification d'une vente/dépense/transaction déjà enregistrée.
- **Bug corrigé** : le bouton Reçu de `paiement_detail_screen.dart` ignorait silencieusement l'erreur retournée par `printRecu()` — aucune erreur n'était jamais affichée à l'utilisateur en cas d'échec. Corrigé en affichant désormais un SnackBar sur erreur.
- **Bug critique corrigé (le flux entier était inopérant)** : `Dio` ne lève **jamais** d'exception pour un statut 2xx — sa `validateStatus` par défaut (`status >= 200 && status < 300`) traite un **202** comme un succès ordinaire. Or toute la détection du sentinel `kApprovalRequiredError` (dans `paiement_state.dart`, `vente_state.dart`, `depense_state.dart`, `transaction_state.dart` — 7 occurrences) était écrite dans le bloc `catch`, jamais atteint pour un 202 : aucune suppression/modification ne demandait donc jamais de PIN pour un rôle sans permission, et aucune erreur n'était affichée — la requête semblait simplement "réussir" sans qu'aucune action n'ait eu lieu côté serveur (`DualAuthChecker` ayant bloqué la route avant d'exécuter quoi que ce soit). Corrigé en vérifiant `response.statusCode == 202` directement après l'`await`, sur le chemin de succès, plutôt que dans le `catch`.
- **Reçu de paiement (`PaiementState.printRecu`)** : `Process.run('open'/'cmd'/'xdg-open', ...)` n'échoue jamais avec une exception sur un exit code non nul — un PDF correctement téléchargé mais qu'aucun lecteur par défaut n'arrivait à ouvrir passait donc pour un succès muet (aucune erreur, aucun visualiseur ne s'ouvre). Corrigé en vérifiant `ProcessResult.exitCode` et en renvoyant l'erreur réelle (`stderr`) si non nul. **Le même motif existe, non corrigé, dans** `vente_state.dart` (`printRecu`), `note_state.dart`, `rapport_state.dart`, `students_state.dart`, `badge_screen.dart` — à appliquer si le même symptôme (PDF généré mais rien ne s'ouvre, sans erreur) y est observé.
- Nouvelle carte "PIN d'autorisation" dans le profil (`profile_screen.dart`), visible uniquement pour les rôles admin/Comptable.

## 7 quinquies. Mise à jour — Payroll horaire, Pointage, double rôle Personnel/Professeur et rapports (livré)

Fonctionnalité ajoutée sur demande explicite (absente de `school_client` et du web) — détail technique complet côté serveur dans `docs/ecole_nginx.md` §9.

- **Payroll** (`payroll_tab.dart`) : formulaire "Verser un salaire" à deux modes — fixe (montant préremplissable depuis le profil de l'employé) ou horaire (sélection des cours du professeur pour une année académique, saisie des heures, montant dû calculé automatiquement contre le taux configuré via "Gérer les taux horaires"). Versements partiels avec solde restant suivi (mirror du module Prêts). Nouveau bouton "Bilan mensuel" (tous les professeurs, montant dû/versé/solde pour un mois donné).
- **Double rôle Personnel/Professeur** ("casquette enseignante") : quand un Personnel a le rôle `teacher`/`Enseignant`, une fiche Professeur liée est créée automatiquement côté serveur et apparaît dans l'écran Professeur avec un badge **"via Personnel"**. Si ce Personnel a aussi un salaire fixe propre, le formulaire "Verser un salaire" affiche un sélecteur **"Professeur" / "Personnel (fixe)"** pour choisir lequel payer (les deux peuvent coexister le même mois, ex. salaire de base + heures supplémentaires).
- **Double rôle symétrique Professeur/Personnel** ("casquette administrative") : quand un Professeur (avec son propre compte) reçoit un rôle non-enseignant (Comptable, Secrétaire général, Responsable pédagogique...) depuis l'onglet Rôles, une fiche Personnel liée est créée automatiquement et apparaît dans l'écran Administration/Personnel avec un badge **"via Professeur"**.
- **Pointage** : nouvel onglet dans la section Présences — liste du personnel du jour (professeurs + personnel), boutons Arrivée/Départ, historique par personne/mois. Sert de référence (affichée, jamais injectée automatiquement) dans le calcul du payroll horaire.
- **Rapports ajoutés** : "Imprimer horaire" (emploi du temps par classe — reconstruit depuis zéro dans `cours_screen.dart`, le bouton existait sur le web mais appelait une route serveur inexistante), "Charge d'enseignement" (nouveau bouton, cours/classes assignés à un professeur pour une année — pas une durée en heures, `Programme.heure` n'a pas de donnée fiable aujourd'hui), "Rapport Payroll" et "Historique des salaires" (nouvelles cartes dans `rapport_screen.dart`, intervalle de dates arbitraire, filtre Global/Professeur/Personnel pour le premier).
- **Bug Flutter récurrent documenté** (`DropdownButtonFormField.initialValue`) : ce widget ne relit sa valeur initiale qu'à sa création — si le champ se construit avant que sa liste d'options (chargée en arrière-plan, ex. `ProgrammeState.loadCombos()`) soit prête, il se fige sur "aucune sélection" et ignore silencieusement toute valeur correcte des rebuilds suivants. Touché à plusieurs reprises sur ce chantier : formulaire Programme en édition (Cours/Professeur/Niveau/Classe/Année — `programme_form_screen.dart`) et formulaire Payroll horaire (Année académique — `payroll_tab.dart`). **Fix systématique à réappliquer si le symptôme réapparaît ailleurs** : donner au widget une `Key` qui change une fois la liste chargée (ex. `ValueKey('cours-${id}-${liste.length}')`), forçant Flutter à recréer le widget avec la bonne valeur au lieu de le laisser bloqué sur son état de construction initial.

## 7 sexies. Mise à jour — recherche, permissions d'impression, correctifs (livré)

Détail technique complet côté serveur dans `docs/ecole_nginx.md` §10 (bug racine du dropdown Programme, bug de journalisation, mapping exact des permissions).

- **Vrai bug racine du dropdown Programme "Cours/Professeur toujours vide"** : après 3 rounds de correctifs `ValueKey` (staleness Flutter, réels pour Niveau/Faculté/Classe/Année), le symptôme persistait sur Cours/Professeur pour une raison entièrement différente et jamais côté client — le schéma de réponse du serveur (`ProgrammeResponse`) ne déclarait pas les clés que la route renvoyait réellement (`profId`/`coursId`), donc FastAPI les supprimait silencieusement de chaque réponse. Corrigé côté serveur ; aucun changement Flutter nécessaire, le modèle `Programme.fromJson` vérifiait déjà les deux variantes de clé.
- **Recherche par saisie sur les listes longues** : nouveau widget réutilisable `SearchableDropdownField<T>` (`lib/widgets/searchable_dropdown_field.dart`, basé sur `Autocomplete<T>`, même motif que la recherche d'étudiants déjà existante) appliqué à : "Professeur/Personnel" du formulaire Payroll, "Cours" du formulaire de taux horaires, "Professeur" du formulaire Programme, "Cours/Matière" et "Changer de matière" du formulaire de saisie de notes.
- **Écran de saisie des notes (`note_entry_screen.dart`)** : thème clair désormais appliqué (cartes utilisaient par erreur `AppColors.sidebarBg`, volontairement toujours sombre, au lieu de `AppColors.cardBg`) ; changer de matière réinitialise maintenant aussi le champ Évaluation et la colonne Note du tableau (auparavant seule la valeur interne était réinitialisée, pas l'affichage — même piège `initialValue` que `DropdownButtonFormField`) ; en-tête réorganisé sur une seule ligne (fiches + les deux champs alignés à droite) ; la puce "Passage" n'apparaît plus que pour les niveaux Technique/Universitaire.
- **Même bug de thème (`sidebarBg` au lieu de `cardBg`) retrouvé et corrigé** dans `presence/appel_tab.dart`, `presence/stats_tab.dart`, `cours/cours_form_screen.dart`, `cours/programme_form_screen.dart`, `finance/vente_composer_screen.dart`, `paiement/paiement_form_screen.dart` (ce dernier avec `panelBg` pour l'avatar de remplacement, pas `cardBg`).
- **Permissions d'impression** : chaque bouton/flux d'impression (reçus paiement/vente/inscription, bulletin simple/en masse, les 8 cartes de l'écran Rapport, horaire/charge d'enseignement) vérifie désormais la permission RBAC correspondante via le nouveau `canPrintPermission()` (`lib/core/print_permission.dart`, même motif que `canPrintNonReceipt()`/`print_gate.dart` — un garde de licence distinct, indépendant). Les impressions automatiques déclenchées après l'enregistrement d'un paiement/d'une vente sont sautées silencieusement si la permission manque, plutôt que d'échouer bruyamment sur un effet secondaire.
- **Grille des permissions** (Profile → Permissions) réorganisée par catégorie (verbe d'action : Ajouter/Modifier/Supprimer/Voir/Imprimer) en 5 cartes disposées sur une grille responsive de 4 colonnes, plutôt qu'une liste plate dans un ordre arbitraire.
- **Nouvel écran "À Propos"** (`lib/screens/apropos/a_propos_screen.dart`) : contenu éditorial (présentation d'Infini Software, mission/vision/valeurs) remplaçant l'ancien `showAboutDialog` générique (nom + version seulement, déjà visibles dans le pied de page persistant). Bug corrigé au passage : la première version utilisait `const AProposScreen()` dans `_buildPage()` — un widget `const` est canonicalisé par Dart, Flutter le reconnaît comme strictement identique d'un rebuild à l'autre et saute l'appel à `build()`, donc ignorait tout changement de thème survenu après le tout premier affichage.
- **Écran de connexion réactif au thème** : `AppColors.loginBg` était une couleur fixe ("toujours sombre", pensée à l'origine pour calquer `school_client`) — le fond de `login_screen.dart`/`first_login_password_screen.dart` suit désormais le thème clair/sombre choisi, comme le reste de l'app ; seul l'accent doré (`loginGold`) reste constant dans les deux thèmes.

## 7 septies. Mise à jour — éditeur visuel de badge (livré)

Nouvel écran séparé (`screens/etudiant/badge_builder/`, point d'entrée `_Mode.badgeBuilder` dans `etudiant_screen.dart`), sans aucun lien avec le flux de génération de badge existant (`badge_screen.dart`/`widgets/badge_renderer.dart`, tous deux laissés inchangés à dessein) — un gabarit construit ici devient sélectionnable dans `badge_screen.dart` au même titre que les 2 templates fixes historiques. Modèle (`models/badge_layout.dart`) et persistance (`services/badge_layout_store.dart`, JSON local) totalement distincts.

- **Canevas de positionnement libre** (`badge_builder_canvas.dart`) : éléments texte (jetons `{{nom}}`/`{{prenom}}`/`{{classe}}`/`{{identifiant}}`/`{{expiration}}`/`{{salle}}` ou texte libre), emplacement photo, QR code, formes/traits (rectangle, arrondi, ovale, ligne, courbe, tracé libre), image statique (logo/signature) — déplacement, redimensionnement (poignées de coin ET de bord, chacune pouvant étirer un seul axe), rotation.
- **Outil plume** (façon Illustrator, poignées de courbure entrante/sortante) pour tracer une forme libre fermée ou ouverte. **Édition ultérieure** : un tracé déjà posé peut être retouché via le bouton "Modifier les points" du panneau de propriétés — cliquer sur le tracé insère un nouveau point d'ancrage à cet endroit (recherche par échantillonnage sur les segments cubiques, `insertPathPoint`), glisser une ancre existante la déplace ; fonctionne correctement même sur un élément pivoté. Le point inséré hérite de VRAIES poignées de courbure (subdivision de De Casteljau du segment cubique visé, qui reproduit exactement la courbe d'origine) quand le segment cliqué en a déjà — un simple coin sans poignée sur un segment droit, pour ne jamais introduire de courbure parasite. Chaque poignée de courbure existante est elle-même glissable pour régler l'arc. Ctrl + clic droit sur une ancre la supprime (comme "supprimer un point d'ancrage" dans Illustrator), pour corriger un point mal placé sans reprendre tout le tracé — refuse de descendre sous 2 points.
- **Découpe permanente entre formes** ("Soustraire du fond") : un bouton (pas une case à cocher persistante — voir la doc de `BadgeElement.subtractBackground` pour l'historique de cette décision) grave immédiatement un trou définitif dans chaque élément recouvert, en dessous en ordre de peinture — contrairement à un masque dynamique recalculé en direct, déplacer ou supprimer la forme découpeuse après coup n'efface plus le trou.
- **Panneau des calques** (`badge_builder_layers_panel.dart`, onglet à bascule avec le panneau Propriétés pour ne pas réduire l'espace du canevas) : masquer un calque (invisible ET absent de l'export, comme dans Illustrator/Photoshop) ou le verrouiller (protégé contre toute découpe reçue d'un autre calque — utile pour un fond qu'on veut garder intact).
- **Règles avec repères glissables** (guides, style Adobe/Figma) : glisser depuis une règle crée un repère horizontal/vertical, repositionnable, supprimé s'il est relâché hors du canevas — purement visuel (jamais exporté), mais persisté avec le gabarit.
- **Export** : PNG par face, PDF recto-verso, ainsi qu'un chemin de génération réel `badge/<année>/<classe>/<nom>.png` (`core/badge_output_path.dart`), cohérent avec `school_client`. Résolution 300dpi injectée manuellement dans le PNG (`widgets/png_dpi.dart`, chunk `pHYs`) — `dart:ui` n'embarque aucune métadonnée de résolution de lui-même.
- **Limite d'environnement documentée** (pas un choix produit) : aucune vérification visuelle du rendu final n'a été possible en environnement de développement sandboxé sans affichage — validée par l'utilisateur en conditions réelles à chaque itération.
- **22 gabarits "pro" prêts à l'emploi** (`services/badge_layout_seed_templates_pro.dart`) : reproduction du STYLE (palette, structure, libellés, motifs décoratifs) de 11 modèles de carte professionnelle fournis en référence, chacun en portrait ET paysage, recto-verso quand la référence montrait les deux faces — semés une seule fois via leur propre marqueur (`.badge_layouts_seeded_pro`, distinct de celui des 10 gabarits abstraits déjà existants) pour apparaître même sur une installation déjà lancée avant leur ajout. Deux écarts assumés faute d'équivalent dans le modèle : un vrai code-barres devient un QR code fonctionnel, une découpe photo hexagonale/losange devient une photo circulaire + un contour décoratif séparé. **Itération geométrique vérifiée localement** : `dart:ui`/`flutter run` étant indisponibles pour un contrôle visuel direct dans cet environnement, les formes en tracé libre (bandeau en vague à double courbe + ligne d'écho, pied de page à coins arrondis) ont été reproduites et rendues avec Pillow (Python) pour comparaison directe avant d'être écrites dans le gabarit réel.

## 7 octies. Mise à jour — suppression de notes pour un seul étudiant (livré)

Détail technique complet côté serveur dans `docs/ecole_nginx.md` §12. Le bouton "Supprimer notes" (`screens/notes/notes_screen.dart::_DeleteNotesDialog`) supprimait déjà des notes en masse par niveau/classe/année/mois — aucun second bouton n'a été ajouté, la même boîte de dialogue accepte désormais un critère supplémentaire.

- En tant qu'utilisateur habilité ("Supprimer note"), je peux restreindre une suppression de notes à un seul étudiant de la classe choisie, en renseignant son identifiant dans un champ optionnel ("Étudiant (optionnel)") — laissé vide, le comportement d'origine (toute la classe) est strictement inchangé.
- Modifier ce champ invalide l'aperçu déjà obtenu, exactement comme les 4 filtres existants (niveau/classe/année/mois) — jamais de suppression sur la base d'une sélection différente de celle vérifiée à l'écran. Le message de confirmation nomme l'étudiant quand il est ciblé.
- Un identifiant blanc/espaces n'est jamais transmis au serveur (traité comme absent) — un vrai bug attrapé par les tests (`test/state/note_state_delete_test.dart`) avant livraison : le premier filtre (`isNotEmpty` sans `trim()`) laissait passer une valeur uniquement composée d'espaces.
- Même contrat d'API des deux côtés : le web (`ecole_nginx/frontend/src/views/admin/Notes.vue`) expose le même champ optionnel, avec la même règle de trim/omission.
- **Tests ajoutés** (aucun n'existait pour `NoteState`/cet écran auparavant) : `test/state/note_state_delete_test.dart` (faux `HttpClientAdapter` — pas de nouvelle dépendance de mock — vérifiant la présence/absence du paramètre et la gestion d'erreur serveur) et `test/screens/notes_delete_dialog_test.dart` (widget test couvrant la présence du champ et l'invalidation de l'aperçu).

## 7 nonies. Mise à jour — promotions préscolaire/bourse, connexion cloud, dashboard, Vues Étudiant (livré)

Détail technique complet côté serveur dans `docs/ecole_nginx.md` §13.

- **Promus (`promus_screen.dart`/`promus_state.dart`)** : les classes préscolaires (sans notes) sont promues automatiquement — la colonne MOY G. affiche "Préscolaire" et le statut devient une puce cliquable "Promu"/"Redouble" pour garder manuellement un enfant en arrière (`setForcerRedoublant`). Nouvelle colonne "AIDE FINANCIÈRE" (menu déroulant) pour modifier la bourse d'un étudiant directement depuis cet écran, envoyée en diff (`pendingAideFinanciere`) au moment de valider la promotion. **Bug corrigé** : les `DataRow` du tableau n'avaient pas de `Key` stable (`key: ValueKey(e.id)` ajouté) — un des symptômes generiques connus de `DropdownButtonFormField` (voir §7 quinquies) pouvant faire dériver la valeur affichée d'une ligne vers une autre lors d'un rebuild.
- **Dashboard (`dashboard_screen.dart`)** : nouveau sélecteur d'année académique dans l'en-tête (équivalent du `combo_anne_for_dash` de `school_client`, absent jusqu'ici) — initialisé sur l'année marquée `status: true`, recharge le dashboard sur l'année choisie sans quitter l'écran.
- **Connexion cloud/local (`api_client.dart`, `login_screen.dart`, `ip_storage.dart`)** : l'écran de connexion propose désormais deux modes explicites avec bouton radio chacun (local sélectionné par défaut) : "Modifier l'ip" (réseau local, TLS pinné sur la CA `aplekol360.local`) et un nouveau champ "Cloud" (lien complet fourni par l'hébergeur, ex. `https://admin.domaine.com/chemin-api` — pas seulement un domaine, le chemin d'API n'est pas forcément `/api/v1`) avec validation TLS standard (CA publique). Le mode choisi et l'URL cloud sont persistés (`SharedPreferences`). **Point technique** : changer de mode remplace entièrement `httpClientAdapter` par un nouvel objet plutôt que de muter `createHttpClient` sur l'existant — un `IOHttpClientAdapter` construit lazy son `HttpClient` interne une seule fois, toute réassignation après le premier usage est silencieusement ignorée.
- **Permission "Autorisation cloud"** : une connexion réussie en mode Cloud sans cette permission est rejetée après coup côté client (`AuthState.login()`), sans laisser de trace (aucun token sauvegardé, aucun état retenu) — voir `docs/ecole_nginx-PRD.md` §7 octies.
- **Vues Étudiant** : `kSubNavItems['etudiant']` étendu de 1 à 10 sous-onglets (Ajouter étudiant, Importer, Diplôme, Certificat, Badge (bouton), Construire la badge, Voir/Modifier/Supprimer par ligne, Badge par ligne) — `etudiant_screen.dart` gate chaque bouton/icône via `subOk(id)`, en plus des permissions `canWrite` déjà en place (jamais en remplacement). Portées côté web sur les 7 sous-onglets ayant un équivalent réel (`docs/ecole_nginx-PRD.md` §7 octies) ; les 2 propres au constructeur de badge restent listées dans les Vues web (base de rôles partagée) sans bouton correspondant à restreindre.
- **Bug corrigé — saisie des notes limitée à Mois/Session** (`note_entry_screen.dart`) : le type d'évaluation (hors Universitaire) était codé en dur sur la liste des mois, quel que soit le niveau — un niveau configuré en "Contrôle" ou "Trimestre" dans Paramètres > Examens (`ParamExam.evaluation_par`, déjà lu et appliqué par le web) recevait quand même le sélecteur mensuel côté bureau. `NoteSearchResult` lit désormais `examEcheance.evaluation_par` (déjà renvoyé par `POST cours-etudiant-add-note`, jusqu'ici ignoré côté client) et affiche la bonne liste de périodes (Mois, ou Contr. I/II/III, ou Trimestre I/II/III — mêmes valeurs que `evalOptions` dans `NoteForm.vue`), et transmet la vraie valeur de `controle` au lieu de `'mois'` fixe. Concerne tout niveau non-Universitaire, y compris un niveau nouvellement ajouté (ex. Secondaire). **Aucun repli silencieux** : si `evaluation_par` est absent/`null` ou une valeur non reconnue (config manquante dans Paramètres > Examens pour ce niveau × année), la saisie est bloquée — message d'erreur explicite affiché à la place du sélecteur, boutons "Enregistrer" désactivés — plutôt que de retomber sur la saisie mensuelle par défaut (en pratique, le serveur bloque déjà à la recherche pour tout niveau non-Universitaire sans `ParamExam`, ce blocage client est une seconde ligne de défense).
