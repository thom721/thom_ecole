BASE_URL2=https://www.institutionlemignon.com
STORAGE_PATH=/static/

# Connexion MySQL — à adapter selon la machine (Mac/Windows/Linux).
# Valeurs d'exemple uniquement : remplace DB_PASSWORD par le vrai mot de
# passe local dans ton propre .env (jamais commité, voir .gitignore).
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=lemignon
